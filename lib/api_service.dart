// lib/api_service.dart
/// Stub de serviço de API sem dependências externas.
/// Mantido apenas para não quebrar imports antigos.
class ApiService {
  const ApiService();

  // Adicione métodos reais depois (ex.: enviar para backend).
}
